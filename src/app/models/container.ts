export class Container {
  container_id: number;
  user_id:number;
  booking_no: string;
  container_no: string;
  port_of_loading: string;
  port_of_discharge: string;
  container_status_id: string;
  sailing_date:string;
  eta:string;
  courier:string;
}
